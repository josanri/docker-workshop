extends layout

block content
  div(class="px-4 py-5 my-5 text-center")
    img(class="d-block mx-auto mb-4 logo" src="https://www.docker.com/wp-content/uploads/2022/03/Docker-Logo-White-RGB_Vertical.png" alt="Docker Logo" width="180")
    h1(class="display-5 fw-bold text-white") Se acabó
    div(class="col-lg-6 mx-auto")
      p(class="lead mb-0 text-white") Muchas gracias por participar en este taller.
      p(class="lead mb-4 text-white") Déjame explicar unas cosas más y ya me voy.
      p(class="lead mb-4 text-white") Visitas a la página #{visits}
      if lastVisitTimestamp
        p(class="lead mb-3 text-white") Última visita el #{lastVisitTimestamp}.
      else
        p(class="lead mb-3 text-white") No hubo visitas previas.
